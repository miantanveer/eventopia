version: 0.2
phases:
  build:
    commands:
      - cp .env.live .env
      - composer update
      - composer install
  post_build:
    commands:
      - php artisan migrate:fresh --seed
artifacts:
  files:
    # List of files to be included in the build artifact
    - '**/*'
